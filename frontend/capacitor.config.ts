import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.crimsonhummingbirdcrawl',
  appName: 'crimson-hummingbird-crawl',
  webDir: 'dist'
};

export default config;
